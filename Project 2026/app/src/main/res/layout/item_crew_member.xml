<?xml version="1.0" encoding="utf-8"?>
<!-- Single row card shown in RecyclerView lists for crew members -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:background="#1A1A3E"
    android:padding="12dp"
    android:layout_marginBottom="8dp"
    android:gravity="center_vertical">

    <!-- Crew member image (specialization icon) -->
    <ImageView
        android:id="@+id/ivCrewImage"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_marginEnd="12dp"
        android:src="@android:drawable/ic_menu_myplaces"/>

    <!-- Crew member info -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <!-- Name and specialization -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/tvCrewName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Crew Name"
                android:textColor="#FFFFFF"
                android:textSize="15sp"
                android:textStyle="bold"
                android:layout_marginEnd="8dp"/>

            <TextView
                android:id="@+id/tvSpecialization"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Pilot"
                android:textColor="#4FC3F7"
                android:textSize="12sp"/>
        </LinearLayout>

        <!-- Stats row -->
        <TextView
            android:id="@+id/tvStats"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Skill:5 | Res:4 | XP:0"
            android:textColor="#8888AA"
            android:textSize="12sp"
            android:layout_marginTop="2dp"/>

        <!-- Energy bar -->
        <ProgressBar
            android:id="@+id/pbEnergy"
            style="@android:style/Widget.ProgressBar.Horizontal"
            android:layout_width="match_parent"
            android:layout_height="8dp"
            android:layout_marginTop="6dp"
            android:max="100"
            android:progress="100"
            android:progressTint="#4CAF50"/>

        <TextView
            android:id="@+id/tvEnergy"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Energy: 20/20"
            android:textColor="#8888AA"
            android:textSize="11sp"
            android:layout_marginTop="2dp"/>

    </LinearLayout>

    <!-- Checkbox for selecting crew members -->
    <CheckBox
        android:id="@+id/cbSelect"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:buttonTint="#4FC3F7"/>

</LinearLayout>